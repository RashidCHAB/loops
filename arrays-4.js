let array = ['JavaScript', 'EcmaScript', 'Java', 'TypeScript', 'C', 'php', "C++", "C#", "Python", "Kotlin"]
let i = 0;
while (i < array.length) {
    if (array[i].length > 3) console.log(array[i])
    i++
}